import { DashboardPlaceholderPage } from '@/components/dashboard/dashboard-placeholder-page';

export default function DashboardStaffPage() {
  return (
    <DashboardPlaceholderPage
      title="Staff"
      description="Manage team members, responsibilities, and schedule ownership."
      emptyTitle="Staff management is on deck"
      emptyDescription="We already have the sidebar and plan gating ready. The next step is connecting the real staff module."
    />
  );
}
