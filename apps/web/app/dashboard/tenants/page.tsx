import { DashboardPlaceholderPage } from '@/components/dashboard/dashboard-placeholder-page';

export default function DashboardTenantsPage() {
  return (
    <DashboardPlaceholderPage
      title="Tenants"
      description="Monitor all businesses running on the platform."
      emptyTitle="Tenant operations panel is next"
      emptyDescription="We will connect onboarding, suspension controls, and tenant health metrics here."
    />
  );
}
