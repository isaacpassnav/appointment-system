import { DashboardPlaceholderPage } from '@/components/dashboard/dashboard-placeholder-page';

export default function DashboardBookPage() {
  return (
    <DashboardPlaceholderPage
      title="Book Appointment"
      description="Launch a guided flow to book a new appointment."
      emptyTitle="Booking flow is being connected"
      emptyDescription="This module will expose the client-side booking wizard with service, staff, and time-slot selection."
    />
  );
}
